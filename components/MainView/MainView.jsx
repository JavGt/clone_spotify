import { TopBar } from '../TopBar';

const MainView = () => {
	return (
		<main className=' col-span-main row-span-main text-white relative overflow-y-auto'>
			<TopBar />
		</main>
	);
};

export default MainView;
